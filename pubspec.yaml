name: carsigo
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  cupertino_icons: ^1.0.8
  uuid: ^4.3.3
  # ---- DEPENDENCIA QUE FALTABA ----
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # ---- SECCIÓN DE ASSETS QUE FALTABA ----
  assets:
    - .env
